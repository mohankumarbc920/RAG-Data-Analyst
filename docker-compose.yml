version: "3.9"

services:
  rag-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data             # persists uploaded documents
      - ./vectorstore:/app/vectorstore  # persists FAISS index across restarts
    env_file:
      - .env
    restart: unless-stopped
